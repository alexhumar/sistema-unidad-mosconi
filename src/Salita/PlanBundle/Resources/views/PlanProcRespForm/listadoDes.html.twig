{% extends 'PlanBundle::base.html.twig' %}

{#
{% block menu %}
    <div id="menu">
        <ul>
            {% include 'SalitaPacienteBundle:OpcionesMenu:opciones.html.twig' %}

            {% if rol == 'ROLE_ADMINISTRADOR' %}
                <li><a href="{{path('menu_administrador')}}">Menu de {{nombreRol}}</a></li>
            {% endif %}    

            {% if rol == 'ROLE_SECRETARIA' %}
                <li><a href="{{path('menu_secretaria')}}">Menu de {{nombreRol}}</a></li>
            {% endif %}  

            {% if rol == 'ROLE_MEDICO' %}
                <li><a href="{{path('menu_medico')}}">Menu de {{nombreRol}}</a></li>
            {% endif %} 
        </ul>
    </div>
{% endblock %}
#}

{% block contenido %}
  <h1 id="tituloCentro">Planes deshabilitados</h1>
  {% if planes|length > 0 %}
    <table>
      <thead><tr><th>Plan</th><th>Periodicidad</th><th></th><th></th></tr></thead>
      <tbody>
        {% for planprocresp in planes %}
          <div id="planDes">
            <tr><td>{{planprocresp.nombre}}</td>
            <td>{{planprocresp.periodicidad}}</td>
            <td><a href="{{ path('habilitacion_planprocresp', { 'idPlan': planprocresp.id }) }}">Habilitar</a></td>
            <td><a href="{{ path('listado_entregaplanprocresp', { 'idPlan': planprocresp.id }) }}">Listar entregas</a></td></tr>
          </div>
        {% endfor %}
      </tbody>
    </table>
  {% else %}
    <h4 id="nota">- No hay planes deshabilitados -</h4>
  {% endif %}
{% endblock %}