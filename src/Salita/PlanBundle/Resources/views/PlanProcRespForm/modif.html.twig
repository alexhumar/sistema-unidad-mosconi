{% extends "SalitaPlanBundle::base.html.twig" %}

{% block contenido %}
  {# {{ form(form) }} #}
    <h3>Modificación de plan procreacion responsable</h3>
    <br/>
    {{ form_start(form) }}
      {{ form_errors(form) }}
      <div class="form-group col-sm-6">
        {{ form_row(form.periodicidad) }}
      </div>
      <div class="form-group col-sm-1">
        <div class="col-sm-offset-1">
          {{ form_row(form.modificar) }}
        </div>
      </div>
    {{ form_end(form) }}
{% endblock %}