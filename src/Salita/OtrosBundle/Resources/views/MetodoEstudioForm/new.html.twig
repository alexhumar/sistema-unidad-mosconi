{% extends '::base.html.twig' %}

{% block contenido %}
	{{ form(form) }}

	{% for flashMessage in app.session.fashbag.get('mensaje') %}
		<div class="flash-message">
			{{ flashMessage }}
		</div>
	{% endfor %}
{% endblock %}